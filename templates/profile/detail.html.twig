<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Mon profile</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
		<link href="{{ asset('css/style03.css') }}" rel="stylesheet">
	</head>
	<body>

		<!--- Navbar -->
		<nav class="navbar navbar-expand-lg pb-5">
			<div class="container-fluid">
				<a class="navbar-brand" href="{{ path('app_home')}}">
					<img src="{{ asset('images/logo.png') }}" class="logo">
				</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarSupportedContent">
					<ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
					<div class="d-flex">
						<ul class="snip1217">
							<li>
								<a href="{{ path('app_home')}}">HOME</a>
							</li>
							<li>
								<a href="{{ path('app_cv')}}">CV</a>
							</li>
							<li>
								<a href="{{ path('app_portfolio')}}">PORTFOLIO</a>
							</li>
							<li>
								<a href="#">ME CONTACTER</a>
							</li>
							<li>
								<a href="{{ path('admin') }}">Admin</a>
							</li>
						</ul>
					</div>
				</div>
			</div>
		</nav>

		<!--- fin de la Navbar -->


		<div class="container-fluid">
			<div class="container">
				<div class="row">
					{% include "_messages_flash.html.twig" %}

					<div class="col-sm-12 col-md-6">
						<a href="{{ path('profile_image_edit') }}">
							<div class="card mb-5" style="">

								<img {% if (app.user.imagePath is same as null) or (app.user.imagePath is same as "") %} src="https://cdn-images.farfetch-contents.com/13/51/26/01/13512601_21418655_1000.jpg" {% else %} src="{{ app.user.imagePath }}" {% endif %} class="card-img-top" alt="image_profile">
							</div>
						</a>
					</div>

					<div class="col-sm-12 col-md-6">
						<div class="card">
							<div class="card-header">
								<h5>Configuration</h5>
							</div>
							<div class="card-body">
								<a href="{{ path('profile_image_edit') }}" class="btn btn-info w-100 mt-2">Modifier ma photo de profil</a>
								<a href="{{ path('admin') }}" class="btn btn-info w-100 mt-2">Modifier mes infos persos</a>
								<a href="{{ path('profile_edit_password') }}" class="btn btn-info w-100 mt-2">Changer mon mot de passe</a>
								<a href="#" class="btn btn-info w-100 mt-2">A propos</a>
							</div>
						</div>

						<div class="card mt-5">
							<div class="card-header">
								<h5>
									<span class="btn-label btn-light">
										<i class="far fa-address-card"></i>
									</span>Infos persos</h5>
							</div>
							<div class="card-body">
								<p>{{ app.user.nom }}</p>
								<p>{{ app.user.prenom }}</p>
								<p>{{ app.user.adresse }}</p>
								<p>{{ app.user.email }}</p>
								<p>{{ app.user.telephone }}</p>
							</div>
						</div>
					</div>


				</div>

			</div>
		</div><!-- Test --></body>
</html>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script></body></html>
